/**
 * RINOVO — Portfolio Interactions
 * Hold-to-reveal mechanic (David Whyte pattern)
 */

import { setupPortfolioParallax } from './scroll.js';

export function initPortfolio() {
  setupPortfolioParallax();
}
